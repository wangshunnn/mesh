# `@ai-mesh/agent`

Vendor-neutral Agent adapter, session, capability, availability, event, and turn
contracts, plus the deterministic scripted adapter used by tests.

This package has no internal dependencies and knows nothing about Rooms,
attention, storage, or UI. Vendor-specific process and wire behavior belongs in
an adapter provider package.
