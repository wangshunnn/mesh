# `@ai-mesh/evals`

Executable causal and concurrency acceptance scenarios for the Room kernel,
including unordered counting, unrelated activity, exclusive task claims,
idempotency, cursor recovery, and stale-object rebasing.

It depends only on protocol, Room, and runtime so kernel correctness is tested
without product orchestration, concrete Agent vendors, Desktop, or SQLite.
